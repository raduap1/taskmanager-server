{
  "name": "taskmanager-server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "migrate": "npx sequelize-cli db:migrate && npx sequelize-cli db:seed:all",
    "start-production": "nodemon -L app.js",
    "start-development": "nodemon --inspect=0.0.0.0:9229 -L app.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "taskmanager-server"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.21",
    "sequelize-cli": "^6.6.0"
  },
  "dependencies": {
    "cls-hooked": "^4.2.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^5.0.0-beta.1",
    "mysql2": "^3.2.0",
    "sequelize": "^6.29.3"
  }
}
